---
title: "Framer Motion 动画实战：打造流畅的页面入场体验"
description: "使用 Framer Motion 在 Astro 项目中实现平滑的入场动画，包括逐字显示、滚动揭示与页面过渡效果。"
date: 2025-01-05
category: guide
tags: ["Framer Motion", "动画", "React", "用户体验", "前端"]
draft: false
---

## 为什么选择 Framer Motion

Framer Motion 是 React 生态中最强大的动画库之一。它的声明式 API、手势支持和布局动画能力使其成为构建精致 UI 的首选。

## 基础入场动画

```tsx
import { motion } from 'framer-motion';

function FadeIn({ children }: { children: React.ReactNode }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 24 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{
        duration: 0.5,
        ease: [0.25, 0.1, 0.25, 1],
      }}
    >
      {children}
    </motion.div>
  );
}
```

## 滚动触发动画

```tsx
import { motion } from 'framer-motion';

function ScrollReveal({ children, delay = 0 }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 24 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: '-80px' }}
      transition={{ duration: 0.5, delay }}
    >
      {children}
    </motion.div>
  );
}
```

## 交错动画

```tsx
function StaggerList({ items }: { items: string[] }) {
  const container = {
    hidden: { opacity: 0 },
    show: {
      opacity: 1,
      transition: {
        staggerChildren: 0.1,
      },
    },
  };

  const item = {
    hidden: { opacity: 0, x: -20 },
    show: { opacity: 1, x: 0 },
  };

  return (
    <motion.ul variants={container} initial="hidden" animate="show">
      {items.map((text, i) => (
        <motion.li key={i} variants={item}>
          {text}
        </motion.li>
      ))}
    </motion.ul>
  );
}
```

## 数字滚动动画

```tsx
function AnimatedCounter({ end, duration = 2 }: { end: number; duration?: number }) {
  const [count, setCount] = useState(0);
  const ref = useRef(null);
  const inView = useInView(ref, { once: true });

  useEffect(() => {
    if (!inView) return;
    let startTime: number;

    const animate = (timestamp: number) => {
      if (!startTime) startTime = timestamp;
      const progress = Math.min((timestamp - startTime) / (duration * 1000), 1);
      setCount(Math.floor(progress * end));
      if (progress < 1) requestAnimationFrame(animate);
    };

    requestAnimationFrame(animate);
  }, [inView, end, duration]);

  return <span ref={ref}>{count}</span>;
}
```

## 性能建议

1. **使用 `layout` 动画时谨慎**：它可能导致大量重排
2. **`whileInView` 优于 `whileHover`**：滚动触发比 hover 触发更节能
3. **设置 `once: true`**：避免重复触发浪费资源
4. **合理使用 `will-change`**：提前告知浏览器即将变化的属性

## 小结

Framer Motion 让复杂的动画变得简单。结合 Astro 的群岛架构，可以在不牺牲性能的前提下打造精致的用户体验。